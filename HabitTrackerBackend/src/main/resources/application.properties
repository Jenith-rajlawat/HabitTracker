#URI format -postgresql://postgres:[YOUR-PASSWORD]@db.zqvhovpqxqfrhwhfqavu.supabase.co:5432/postgres
#jdbc format: jdbc:postgresql://db.zqvhovpqxqfrhwhfqavu.supabase.co:5432/postgres
spring.datasource.url=${DATABASE_URL}
spring.datasource.username=${DATABASE_USERNAME}
spring.datasource.password=${DATABASE_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

#while committing and deploying do this:
#spring.datasource.url=${DATABASE_URL}
#spring.datasource.username=${DATABASE_USERNAME}
#spring.datasource.password=${DATABASE_PASSWORD}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Previously I let git track this file so to remove it I used
# git rm --cached -f HabitTrackerBackend/src/main/resources/application.properties
# write relative path in gitignore or just ignore all **/application.properties or all properties *.properties

server.port=${PORT:8080}